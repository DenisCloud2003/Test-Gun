using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBodyParts : MonoBehaviour {
    public enum ColliderBodyType { head, body, arms, legs}
    public ColliderBodyType type;

}
